from django.apps import AppConfig


class SizeDiffConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.size_diff"
    verbose_name = "Vixi's Size-Difference Calculator!"
